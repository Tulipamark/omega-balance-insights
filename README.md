# Welcome to your Lovable project

## Local development

Run the app locally with:

```powershell
npm run dev
```

The Vite dev server runs on `http://localhost:8080`.

In VS Code you can also start everything from `Run and Debug`:

- `Open localhost:8080`
- `Open localhost:8080 (Chrome)`

Both launch profiles start the dev server automatically before opening the app.
